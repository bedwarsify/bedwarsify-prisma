{
  "name": "bedwarsify-prisma",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "node scripts/postinstall.js",
    "migrate": "prisma migrate dev --create-only --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "^2.26.0",
    "fs-extra": "^10.0.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.11",
    "prisma": "^2.30.3"
  }
}
